import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'inapp-browser-sample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
